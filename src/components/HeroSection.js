import React from 'react';
import './HeroSection.css'; // Import the CSS file

const HeroSection = () => {
  return (
    <div className="hero-container">
      <div className="hero-text">
        <h1 className="main-heading">Silver Design</h1>
        <h2 className="sub-heading">A design framework that supports senior user needs</h2>
      </div>
    </div>
  );
};

export default HeroSection;
